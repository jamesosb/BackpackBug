<div class="other-content toplift">
<%= render :partial =>'public_activity/shared/prof', :locals => { :a => a } %><%=link_to a.owner.profile.forename, profile_path(a.owner.id) %> added an image to 
<% @locations.each do |locs|%>
<%if locs.id == a.trackable.location_id%>
<%=link_to locs.name, location_path(locs.id)%>
<%end end%>
<%= render :partial =>'public_activity/shared/info', :locals => { :a => a } %>
</div>
