<div class="other-content toplift">
<%= render :partial =>'public_activity/shared/prof', :locals => { :a => a } %><%=link_to a.owner.profile.forename, profile_path(a.owner.id) %> created a new   
<% if a.trackable %>
<%= link_to "profile", profile_path(a.trackable_id) %>
<%else%>which has now been deleted.
<%end%>
<%= render :partial =>'public_activity/shared/info', :locals => { :a => a } %>
</div>
